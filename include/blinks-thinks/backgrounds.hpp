#pragma once

void DrawScrollingCheckerboard (
        int screenWidth, 
        int screenHeight, 
        Color darkColor,
        Color lightColor,
        float scrollOffset, 
        int squareSize
);
